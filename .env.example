# Environment Variables for Terminal Emulator

# Filesystem Mode Configuration
# Choose which filesystem structure to use at deployment time
# Options: 'default' | 'portfolio'
# Default: 'default' (Unix-like filesystem with /home, /etc, /var, etc.)
# Portfolio: Portfolio-focused structure with /about, /projects, /contact, /blog
VITE_FILESYSTEM_MODE=default